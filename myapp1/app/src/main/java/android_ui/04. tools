app 폴더 밑에 src 폴더

androidTest -> 테스트 코드 사용시
main -> 우리가 작성하는 자바코드(코틀린)를 작성할 때 사용한다.

res -> 리소스의 약자 (이미지, 필요한 여러 utils 파일등을 넣어 두는 곳)

drawable -> 이미지를 넣어두는 폴더입니다.
layout -> xml 을 이용해서 화면을 그릴 때 사용하는 파일을 넣어둔다.
minmap -> 앱 아이콘 이미지를 넣어두면 된다.
values -> 공통적으로 사용되는 값들을 넣어두는 곳입니다.